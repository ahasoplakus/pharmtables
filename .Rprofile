source("renv/activate.R")
options(repos = c(
  RSPM = "https://packagemanager.posit.co/cran/__linux__/centos7/latest",
  CRAN = "https://cloud.r-project.org/"
))
options(renv.config.repos.override = getOption("repos"))
